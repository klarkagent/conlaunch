# ConLaunch Configuration

# Required: Private key of the ConLaunch deployer wallet (with 0x prefix)
PRIVATE_KEY=0x...

# Required: API key for authenticated endpoints (deploy, claim, upload)
# Generate with: openssl rand -hex 32
API_KEY=

# Required: Port for the API server
PORT=3000

# Optional: Base RPC URL (defaults to public RPC)
BASE_RPC_URL=https://mainnet.base.org

# ConLaunch platform fee in basis points (default: 2000 = 20%)
PLATFORM_FEE_BPS=2000

# $CONLAUNCH token address (set after deploying the token)
CONLAUNCH_TOKEN_ADDRESS=

# Optional: Comma-separated allowed CORS origins (default: * in dev)
CORS_ORIGINS=https://conlaunch.com

# Optional: Clanker API key (only needed for REST API fallback)
CLANKER_API_KEY=
